stages:
  - test

unit_tests:
  stage: test
  image: python:3.11
  before_script:
    - python --version
    - pip install -r requirements.txt
  script:
    - PYTHONPATH=. pytest test/unit --junitxml=report.xml
  artifacts:
    when: always
    reports:
      junit: report.xml
